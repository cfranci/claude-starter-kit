---
description: Copy the most recent code block, command, or suggested text to clipboard
---
Find the most recent code block, command, or actionable text from your last response. Copy it to the clipboard with `pbcopy`. Show what was copied so the user can verify.
