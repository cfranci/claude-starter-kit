---
description: Quick save — stage and commit all changes with an auto-generated message
---
Run `git diff --stat` and `git status --short`. Auto-generate a concise commit message from the changes. Stage relevant files and commit. Do NOT push unless the user says to. Show the commit message.
