---
description: Run the project (dev server, build, or main script)
---
Detect how to run this project. Check for: `package.json` (npm run dev/start), `Cargo.toml` (cargo run), `pyproject.toml`/`main.py` (python), `Makefile` (make run), `go.mod` (go run). Run the most appropriate dev/start command. If already running, say so.
