---
description: Find a file, function, or string in the codebase
---
Search for `$ARGUMENTS` in the codebase. Try in order: filename match (Glob), then content match (Grep). Show results concisely — file paths with line numbers. If `$ARGUMENTS` is empty, ask what to search for.
