---
description: Quick status — git status, running processes, what we're working on
---
Run `git status --short` and `git log --oneline -3`. Briefly summarize: branch, uncommitted changes, last few commits, and what was being worked on in this conversation. Keep it to 5 lines max.
